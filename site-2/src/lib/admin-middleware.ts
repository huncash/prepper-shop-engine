/**
 * Admin auth helper stub — TanStack middleware helyett.
 * A valódi ellenőrzés AdminGate + későbbi API route-okban történik.
 */
export const requireAdmin = {
  /* placeholder for legacy imports */
};

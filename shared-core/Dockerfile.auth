FROM node:22-alpine

RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY shared-core/package.json ./

RUN npm install

COPY shared-core .

ENV AUTH_HOST=0.0.0.0
ENV AUTH_PORT=4001

EXPOSE 4001

CMD ["npx", "tsx", "src/auth-server.ts"]

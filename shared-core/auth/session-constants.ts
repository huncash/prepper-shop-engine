/** Edge-safe — no Node crypto. */
export const SESSION_COOKIE = "prepper_session";

/** Ügyfél / partner session — soha ne keveredjen az ügyvezetői cookie-val. */
export const PARTNER_SESSION_COOKIE = "prepper_partner_session";
